TODO: (- : to do, + : done, * : optional, > : in progress)

+ ...
> add expressions,
	+ add Aritmethic expression,
	+ add conditional expression,
	+ add null, this, result,
	+ add local variable,
	+ add field access:
		+ add field ref,
		+ add full qualified name.
	+ add olds,
	- add modifies expressions:
		- ident,
		- local variable,
		- array:
			- all array elements,
			- array elements from to,
			- single index.
		- dot,
- add attributes:
	+ add LoopSpecification,
	* add HistoryConstraints,
	* add ParamTable.
