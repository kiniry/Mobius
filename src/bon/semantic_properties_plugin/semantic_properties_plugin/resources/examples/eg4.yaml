---
#corrrect file with imbedded choice and optional
name: concurrency
format:
  - [TIMEOUT, <tol=nat>, fine]
  - choice (hi):
    - SEQUENTIAL
    - GUARDED
    - 
      - CONCURRENT
      - choice:
        -  yes
        - <no=string>
        - <guff=URL>
    - [TIMEOUT, <to=myint>, <e=nat>]
    - [FAILURE, <z=nat>]
    - [SPECIAL, optional: <fun=throwable>]
  - optional: <description=nat>
scope: 
 - Modules
description: |
 des
