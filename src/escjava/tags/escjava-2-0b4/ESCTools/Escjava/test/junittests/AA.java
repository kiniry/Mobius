// This simple test is here just as a smoke test and as a check that
// previously parsed types (along with AAA.java) are being cleared out
// of caches between JUnit tests
public class AA {
	//@ public model int i;
}
