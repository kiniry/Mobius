// preconditions
assume (\forall Example e, o; 
       less(statePre,e,o) == e.value < o.value);
assume less(statePre,min,max);
// body
...
// postconditions
assert (\forall Example e, o; 
       less(statePost,e,o) == e.value < o.value)
       ==> !less(statePost,this,min);
