// This file was generated by jmlunit on Tue Jun 26 18:11:18 CDT 2007.

/** Automatically-generated test driver for JML and JUnit based
 * testing of BagOfInt. The superclass of this class should be edited
 * to supply test data. However it's best not to edit this class
 * directly; instead use the command
 * <pre>
 *  jmlunit BagOfInt.java
 * </pre>
 * to regenerate this class whenever BagOfInt.java changes.
 */
public class BagOfInt_JML_Test
     extends BagOfInt_JML_TestData
{
    /** Initialize this class. */
    public BagOfInt_JML_Test(java.lang.String name) {
        super(name);
    }

    /** Run the tests. */
    public static void main(java.lang.String[] args) {
        org.jmlspecs.jmlunit.JMLTestRunner.run(suite());
        // You can also use a JUnit test runner such as:
        // junit.textui.TestRunner.run(suite());
    }

    /** Test to see if the code for class BagOfInt
     * has been compiled with runtime assertion checking (i.e., by jmlc).
     * Code that is not compiled with jmlc would not make an effective test,
     * since no assertion checking would be done. */
    public void test$IsRACCompiled() {
        junit.framework.Assert.assertTrue("code for class BagOfInt"
                + " was not compiled with jmlc"
                + " so no assertions will be checked!",
            org.jmlspecs.jmlrac.runtime.JMLChecker.isRACCompiled(BagOfInt.class)
            );
    }

    /** Return the test suite for this test class.  This will have
    * added to it at least test$IsRACCompiled(), and any test methods
    * written explicitly by the user in the superclass.  It will also
    * have added test suites for each testing each method and
    * constructor.
    */
    //@ ensures \result != null;
    public static junit.framework.Test suite() {
        BagOfInt_JML_Test testobj
            = new BagOfInt_JML_Test("BagOfInt_JML_Test");
        junit.framework.TestSuite testsuite = testobj.overallTestSuite();
        // Add instances of Test found by the reflection mechanism.
        testsuite.addTestSuite(BagOfInt_JML_Test.class);
        testobj.addTestSuiteForEachMethod(testsuite);
        return testsuite;
    }

    /** A JUnit test object that can run a single test method.  This
     * is defined as a nested class solely for convenience; it can't
     * be defined once and for all because it must subclass its
     * enclosing class.
     */
    protected static abstract class OneTest extends BagOfInt_JML_Test {

        /** Initialize this test object. */
        public OneTest(String name) {
            super(name);
        }

        /** The result object that holds information about testing. */
        protected junit.framework.TestResult result;

        //@ also
        //@ requires result != null;
        public void run(junit.framework.TestResult result) {
            this.result = result;
            super.run(result);
        }

        /* Run a single test and decide whether the test was
         * successful, meaningless, or a failure.  This is the
         * Template Method pattern abstraction of the inner loop in a
         * JML/JUnit test. */
        public void runTest() throws java.lang.Throwable {
            try {
                // The call being tested!
                doCall();
            }
            catch (org.jmlspecs.jmlrac.runtime.JMLEntryPreconditionError e) {
                // meaningless test input
                addMeaningless();
            } catch (org.jmlspecs.jmlrac.runtime.JMLAssertionError e) {
                // test failure
                int l = org.jmlspecs.jmlrac.runtime.JMLChecker.getLevel();
                org.jmlspecs.jmlrac.runtime.JMLChecker.setLevel
                    (org.jmlspecs.jmlrac.runtime.JMLOption.NONE);
                try {
                    java.lang.String failmsg = this.failMessage(e);
                    junit.framework.AssertionFailedError err
                        = new junit.framework.AssertionFailedError(failmsg);
                    err.setStackTrace(new java.lang.StackTraceElement[]{});
                    err.initCause(e);
                    result.addFailure(this, err);
                } finally {
                    org.jmlspecs.jmlrac.runtime.JMLChecker.setLevel(l);
                }
            } catch (java.lang.Throwable e) {
                // test success
            }
        }

        /** Call the method to be tested with the appropriate arguments. */
        protected abstract void doCall() throws java.lang.Throwable;

        /** Format the error message for a test failure, based on the
         * method's arguments. */
        protected abstract java.lang.String failMessage
            (org.jmlspecs.jmlrac.runtime.JMLAssertionError e);

        /** Inform listeners that a meaningless test was run. */
        private void addMeaningless() {
            if (result instanceof org.jmlspecs.jmlunit.JMLTestResult) {
                ((org.jmlspecs.jmlunit.JMLTestResult)result)
                    .addMeaningless(this);
            }
        }
    }

    /** Create the tests that are to be run for testing the class
     * BagOfInt.  The framework will then run them.
     * @param overallTestSuite$ The suite accumulating all of the tests
     * for this driver class.
     */
    //@ requires overallTestSuite$ != null;
    public void addTestSuiteForEachMethod
        (junit.framework.TestSuite overallTestSuite$)
    {
        try {
            this.addTestSuiteFor$TestBagOfInt(overallTestSuite$);
        } catch (java.lang.Throwable ex) {
            overallTestSuite$.addTest
                (new org.jmlspecs.jmlunit.strategies.ConstructorFailed(ex));
        }
        try {
            this.addTestSuiteFor$TestOccurrences(overallTestSuite$);
        } catch (java.lang.Throwable ex) {
            overallTestSuite$.addTest
                (new org.jmlspecs.jmlunit.strategies.ConstructorFailed(ex));
        }
        try {
            this.addTestSuiteFor$TestExtractMin(overallTestSuite$);
        } catch (java.lang.Throwable ex) {
            overallTestSuite$.addTest
                (new org.jmlspecs.jmlunit.strategies.ConstructorFailed(ex));
        }
        try {
            this.addTestSuiteFor$TestMain(overallTestSuite$);
        } catch (java.lang.Throwable ex) {
            overallTestSuite$.addTest
                (new org.jmlspecs.jmlunit.strategies.ConstructorFailed(ex));
        }
        try {
            this.addTestSuiteFor$TestToString(overallTestSuite$);
        } catch (java.lang.Throwable ex) {
            overallTestSuite$.addTest
                (new org.jmlspecs.jmlunit.strategies.ConstructorFailed(ex));
        }
    }

    /** Add tests for the BagOfInt contructor
     * to the overall test suite. */
    private void addTestSuiteFor$TestBagOfInt
        (junit.framework.TestSuite overallTestSuite$)
    {
        junit.framework.TestSuite methodTests$
            = this.emptyTestSuiteFor("BagOfInt");
        try {
            org.jmlspecs.jmlunit.strategies.IndefiniteIterator
                vint$_$1$iter
                = this.vint$_Iter("BagOfInt", 0);
            this.check_has_data
                (vint$_$1$iter,
                 "this.vint$_Iter(\"BagOfInt\", 0)");
            while (!vint$_$1$iter.atEnd()) {
                final int[] input
                    = (int[]) vint$_$1$iter.get();
                methodTests$.addTest
                    (new TestBagOfInt(input));
                vint$_$1$iter.advance();
            }
        } catch (org.jmlspecs.jmlunit.strategies.TestSuiteFullException e$) {
            // methodTests$ doesn't want more tests
        }
        overallTestSuite$.addTest(methodTests$);
    }

    /** Test for the BagOfInt contructor. */
    protected static class TestBagOfInt extends OneTest {
        /** Argument input */
        private int[] input;

        /** Initialize this instance. */
        public TestBagOfInt(int[] input) {
            super("BagOfInt"+ ":" + (input==null?"null":("{int["+input.length + "]"+"}")));
            this.input = input;
        }

        protected void doCall() throws java.lang.Throwable {
            new BagOfInt(input);
        }

        protected java.lang.String failMessage
            (org.jmlspecs.jmlrac.runtime.JMLAssertionError e$)
        {
            java.lang.String msg = "\n\tContructor 'BagOfInt' applied to";
            msg += "\n\tArgument input: " + this.input;
            return msg;
        }
    }

    /** Add tests for the occurrences method
     * to the overall test suite. */
    private void addTestSuiteFor$TestOccurrences
        (junit.framework.TestSuite overallTestSuite$)
    {
        junit.framework.TestSuite methodTests$
            = this.emptyTestSuiteFor("occurrences");
        try {
            org.jmlspecs.jmlunit.strategies.IndefiniteIterator
                receivers$iter
                = new org.jmlspecs.jmlunit.strategies.NonNullIteratorDecorator
                    (this.vBagOfIntIter("occurrences", 1));
            this.check_has_data
                (receivers$iter,
                 "new NonNullIteratorDecorator(this.vBagOfIntIter(\"occurrences\", 1))");
            while (!receivers$iter.atEnd()) {
                org.jmlspecs.jmlunit.strategies.IntIterator
                    vint$1$iter
                    = this.vintIter("occurrences", 0);
                this.check_has_data
                    (vint$1$iter,
                     "this.vintIter(\"occurrences\", 0)");
                while (!vint$1$iter.atEnd()) {
                    final BagOfInt receiver$
                        = (BagOfInt) receivers$iter.get();
                    final int i
                        = vint$1$iter.getInt();
                    methodTests$.addTest
                        (new TestOccurrences(receiver$, i));
                    vint$1$iter.advance();
                }
                receivers$iter.advance();
            }
        } catch (org.jmlspecs.jmlunit.strategies.TestSuiteFullException e$) {
            // methodTests$ doesn't want more tests
        }
        overallTestSuite$.addTest(methodTests$);
    }

    /** Test for the occurrences method. */
    protected static class TestOccurrences extends OneTest {
        /** The receiver */
        private BagOfInt receiver$;
        /** Argument i */
        private int i;

        /** Initialize this instance. */
        public TestOccurrences(BagOfInt receiver$, int i) {
            super("occurrences"+ ":" + i);
            this.receiver$ = receiver$;
            this.i = i;
        }

        protected void doCall() throws java.lang.Throwable {
            receiver$.occurrences(i);
        }

        protected java.lang.String failMessage
            (org.jmlspecs.jmlrac.runtime.JMLAssertionError e$)
        {
            java.lang.String msg = "\n\tMethod 'occurrences' applied to";
            msg += "\n\tReceiver: " + this.receiver$;
            msg += "\n\tArgument i: " + this.i;
            return msg;
        }
    }

    /** Add tests for the extractMin method
     * to the overall test suite. */
    private void addTestSuiteFor$TestExtractMin
        (junit.framework.TestSuite overallTestSuite$)
    {
        junit.framework.TestSuite methodTests$
            = this.emptyTestSuiteFor("extractMin");
        try {
            org.jmlspecs.jmlunit.strategies.IndefiniteIterator
                receivers$iter
                = new org.jmlspecs.jmlunit.strategies.NonNullIteratorDecorator
                    (this.vBagOfIntIter("extractMin", 0));
            this.check_has_data
                (receivers$iter,
                 "new NonNullIteratorDecorator(this.vBagOfIntIter(\"extractMin\", 0))");
            while (!receivers$iter.atEnd()) {
                final BagOfInt receiver$
                    = (BagOfInt) receivers$iter.get();
                methodTests$.addTest
                    (new TestExtractMin(receiver$));
                receivers$iter.advance();
            }
        } catch (org.jmlspecs.jmlunit.strategies.TestSuiteFullException e$) {
            // methodTests$ doesn't want more tests
        }
        overallTestSuite$.addTest(methodTests$);
    }

    /** Test for the extractMin method. */
    protected static class TestExtractMin extends OneTest {
        /** The receiver */
        private BagOfInt receiver$;

        /** Initialize this instance. */
        public TestExtractMin(BagOfInt receiver$) {
            super("extractMin");
            this.receiver$ = receiver$;
        }

        protected void doCall() throws java.lang.Throwable {
            receiver$.extractMin();
        }

        protected java.lang.String failMessage
            (org.jmlspecs.jmlrac.runtime.JMLAssertionError e$)
        {
            java.lang.String msg = "\n\tMethod 'extractMin' applied to";
            msg += "\n\tReceiver: " + this.receiver$;
            return msg;
        }
    }

    /** Add tests for the main method
     * to the overall test suite. */
    private void addTestSuiteFor$TestMain
        (junit.framework.TestSuite overallTestSuite$)
    {
        junit.framework.TestSuite methodTests$
            = this.emptyTestSuiteFor("main");
        try {
            org.jmlspecs.jmlunit.strategies.IndefiniteIterator
                vjava_lang_String$_$1$iter
                = this.vjava_lang_String$_Iter("main", 0);
            this.check_has_data
                (vjava_lang_String$_$1$iter,
                 "this.vjava_lang_String$_Iter(\"main\", 0)");
            while (!vjava_lang_String$_$1$iter.atEnd()) {
                final java.lang.String[] argv
                    = (java.lang.String[]) vjava_lang_String$_$1$iter.get();
                methodTests$.addTest
                    (new TestMain(argv));
                vjava_lang_String$_$1$iter.advance();
            }
        } catch (org.jmlspecs.jmlunit.strategies.TestSuiteFullException e$) {
            // methodTests$ doesn't want more tests
        }
        overallTestSuite$.addTest(methodTests$);
    }

    /** Test for the main method. */
    protected static class TestMain extends OneTest {
        /** Argument argv */
        private java.lang.String[] argv;

        /** Initialize this instance. */
        public TestMain(java.lang.String[] argv) {
            super("main"+ ":" + (argv==null?"null":("{java.lang.String["+argv.length + "]"+"}")));
            this.argv = argv;
        }

        protected void doCall() throws java.lang.Throwable {
            BagOfInt.main(argv);
        }

        protected java.lang.String failMessage
            (org.jmlspecs.jmlrac.runtime.JMLAssertionError e$)
        {
            java.lang.String msg = "\n\tMethod 'main' applied to";
            msg += "\n\tReceiver class BagOfInt";
            msg += "\n\tArgument argv: " + this.argv;
            return msg;
        }
    }

    /** Add tests for the toString method
     * to the overall test suite. */
    private void addTestSuiteFor$TestToString
        (junit.framework.TestSuite overallTestSuite$)
    {
        junit.framework.TestSuite methodTests$
            = this.emptyTestSuiteFor("toString");
        try {
            org.jmlspecs.jmlunit.strategies.IndefiniteIterator
                receivers$iter
                = new org.jmlspecs.jmlunit.strategies.NonNullIteratorDecorator
                    (this.vBagOfIntIter("toString", 0));
            this.check_has_data
                (receivers$iter,
                 "new NonNullIteratorDecorator(this.vBagOfIntIter(\"toString\", 0))");
            while (!receivers$iter.atEnd()) {
                final BagOfInt receiver$
                    = (BagOfInt) receivers$iter.get();
                methodTests$.addTest
                    (new TestToString(receiver$));
                receivers$iter.advance();
            }
        } catch (org.jmlspecs.jmlunit.strategies.TestSuiteFullException e$) {
            // methodTests$ doesn't want more tests
        }
        overallTestSuite$.addTest(methodTests$);
    }

    /** Test for the toString method. */
    protected static class TestToString extends OneTest {
        /** The receiver */
        private BagOfInt receiver$;

        /** Initialize this instance. */
        public TestToString(BagOfInt receiver$) {
            super("toString");
            this.receiver$ = receiver$;
        }

        protected void doCall() throws java.lang.Throwable {
            receiver$.toString();
        }

        protected java.lang.String failMessage
            (org.jmlspecs.jmlrac.runtime.JMLAssertionError e$)
        {
            java.lang.String msg = "\n\tMethod 'toString' applied to";
            msg += "\n\tReceiver: " + this.receiver$;
            return msg;
        }
    }

    /** Check that the iterator is non-null and not empty. */
    private void
    check_has_data(org.jmlspecs.jmlunit.strategies.IndefiniteIterator iter,
                   String call)
    {
        if (iter == null) {
            junit.framework.Assert.fail(call + " returned null");
        }
        if (iter.atEnd()) {
            junit.framework.Assert.fail(call + " returned an empty iterator");
        }
    }

    /** Converts a char to a printable String for display */
    public static String charToString(char c) {
        if (c == '\n') {
            return "NL";
        } else if (c == '\r') {
            return "CR";
        } else if (c == '\t') {
            return "TAB";
        } else if (Character.isISOControl(c)) {
            int i = (int)c;
            return "\\u"
                    + Character.forDigit((i/2048)%16,16)
                    + Character.forDigit((i/256)%16,16)
                    + Character.forDigit((i/16)%16,16)
                    + Character.forDigit((i)%16,16);
        }
        return Character.toString(c);
    }
}
