% Argument of pre- and postcondition, and set annotations is either value or
% exception.
% For convenience, modeled as single datatype
ValOrExcpt[Name : TYPE+] : DATATYPE
BEGIN

  IMPORTING ValProp[Name], Excpt, Body[Name]

  val(v : Expr) : val?
  excpt (e : Excpt) : excpt?

END ValOrExcpt

